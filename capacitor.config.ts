import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.workspacestudio.app',
  appName: 'Workspace Studio',
  webDir: 'dist',
};

export default config;
